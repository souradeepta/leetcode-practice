import collections

def sol():
    result = []
    if(not bids or not totalShares):
        return result

    # de = collections.dequeu()
    # for bid in bids:
    #     de.append(bid)
    sortedBids = []
    # for i in range(len(bids)-2):
    #     if bid[2] > bid[2]+1:
    #         sortedBids.append(bid)
    #     elif bid[2] == bid[2]+1:
    #         if bid[3] > bid[3]+1:
    #             sortedBids.append

    